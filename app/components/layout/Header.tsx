import '../auth/auth.css'

const Header = () => {
    return (
        <header>
            <div className="bg-[url('/lace.svg')] bg-repeat h-[76px]" />
        </header>
    );
};

export default Header;
